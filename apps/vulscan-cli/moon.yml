$schema: "https://moonrepo.dev/schemas/project.json"

id: "vulscan-cli"
type: "application"
language: "go"
tags: ["cli", "go"]

tasks:
  build:
    command: "go build -o bin/vulscan ./main.go"
    inputs:
      - "**/*.go"
      - "go.mod"
      - "go.sum"
    outputs:
      - "bin/vulscan"

  test:
    command: "go test ./..."
    inputs:
      - "**/*.go"
      - "go.mod"
      - "go.sum"

  lint:
    command: "go vet ./..."
    inputs:
      - "**/*.go"
